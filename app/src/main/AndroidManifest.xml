<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tech.iwish.ONhome">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SignUpActivity" android:label="Sign Up"></activity>
        <activity android:name=".MyAccount" android:label="My Account" />
        <activity android:name=".Myorders" android:label="My Orders" />
        <activity android:name=".Order_Recieved" android:label="Order Receive" />
        <activity android:name=".OrderConfirm" android:label="Order Confirm" />
        <activity android:name=".ConfirmOrder" android:label="Confirm Order" />
        <activity android:name=".OrderDetail" android:label="Order Detail" />
        <activity android:name=".LoginActivity" android:label="Login" />
        <activity android:name=".CartActivity" android:label="My Cart" />
        <activity android:name=".ProductSearchActivity" android:label="Search All Products" />

        <activity
            android:name=".HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activity.SplashScreen"
            android:label="@string/title_activity_splash_screen"
            android:theme="@style/AppTheme.NoActionBar.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.MainActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ItemDescription"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>